# 05 — Shimmer Loading Skeletons

**What to build:**
Replace standard animate-pulse loading blocks with metallic shimmer sweep placeholders across HEALTH website.

**Blocked by:** 01 — Global Tokens & Keyframe Animations.

**Status:** ready-for-agent

- [ ] Apply `.animate-shimmer` to news skeleton placeholders and image loading states.
- [ ] Ensure 300ms `ease-out` smooth image cross-fade on load completion.
- [ ] Run full type checking (`npx tsc --noEmit`) and build verification (`npm run build`).
